# Parameter Golf PR Scout Report
Generated: 2026-03-19T20:24:08-07:00

**Our current stack:** 9L/512d, MLP 3x, int6+zstd, fp16 embed, STE QAT, NorMuon, SWA,
SmearGate, MTP, sliding window (stride 256), TTT (full-param SGD, prefix/suffix split).
**Our best BPB:** 1.2244 (H100 baseline), 1.162 SW (4090 1800s).


---
## PR #120 — [Val Only]: MLP 3x + STE int6 QAT + sliding window, val_bpb=0.9588
**Author:** andrewgcodes | **State:** CLOSED | **BPB:** 0.9588 | **Diff:** 1246 lines

TECHNIQUES:
- [TRAIN] MLP 3x expansion: hidden=1536 (3×512), ReLU²activation. Delta: unknown alone
- [TRAIN] STE fake-int6 QAT: straight-through estimator rounds weights to int6 grid during forward, gradients flow through unquantized. Per-row clipping at 99.99984th percentile
- [EVAL] Sliding window eval: stride=64, windows of seq_len=4096, only last `stride` tokens scored per window (first window scores all). Much tighter than standard
- [POST] Mixed quantization: int6 per-row for weight matrices, int8 per-row for tok_emb/lm_head, then zlib compress
- [TRAIN] Muon momentum warmup: ramps from 0.92→0.99 over 1500 steps
- [TRAIN] Longer warmdown: 14000/20000 iters (70% of run)
- [ARCH] resid_mix learnable per-layer blend of x and x0 (skip connection to embedding)
- [ARCH] q_gain per-head scalar on queries, initialized 1.5

KEY_INNOVATIONS:
- Sliding window stride=64 vs our stride=256 — 4× more overlap means nearly every token sees full context; this alone likely accounts for most of the reported BPB gain (eval metric improvement, not model improvement)
- STE int6 QAT: quantization noise during training regularizes weights toward the post-quant weight distribution, reducing the train→quant BPB gap

WE_DONT_HAVE:
- Sliding window eval with stride=64 (we use stride=256 — this is a direct scoring improvement)
- STE fake-int6 QAT in `CastedLinear.forward` (we have QAT but need to confirm it's the same STE approach)
- resid_mix: learnable per-layer blend `mix[0]*x + mix[1]*x0` (we have SmearGate but may differ)
- Muon momentum warmup from 0.92→0.99 over 1500 steps

WE_SHOULD_STEAL:
1. **Sliding window stride=64** (easy, eval-only change): Pure metric improvement with no training cost. Going from stride=256→64 gives 4× more context per scored token. This is the most likely explanation for their 0.9588 vs our current number. Difficulty: easy.
2. **STE int6 QAT in CastedLinear** (medium): If our QAT differs, adopting the exact per-row percentile clipping + round-then-STE pattern closes the train/quant gap. The implementation is clean and self-contained. Difficulty: medium (verify we don't already have equivalent).


---
## PR #64 — Record: val_bpb=0.9695 (val-only) + val_bpb=1.1629 (standard)
**Author:** yesbhautik | **State:** OPEN | **BPB:** 0.9695 | **Diff:** 2493 lines

**TECHNIQUES:**
- [TRAIN] STE fake-int6 QAT: Straight-Through Estimator fakes int6 quantization during forward pass (weights fake-quantized to [-31,31]), reducing post-quant penalty from ~0.05 to ~0.001 BPB. Delta: ~0.05 BPB
- [TRAIN] Tuned Muon: momentum=0.99, LR=0.02, warmdown=3000 steps, seq_len=4096. Standard Muon tuning.
- [TRAIN] MLP 3x expansion: hidden=1536 (3×512), ~21.8M params total.
- [TRAIN] Val-only training (CombinedOptimal): train on val set only, organizer-approved separate category. Delta: ~0.19 BPB vs standard
- [EVAL] Sliding window eval stride=64: every token scored with 960+ tokens of context. Delta vs no sliding window: significant; vs stride=256: moderate
- [QUANT] Mixed PTQ: int6 per-row on transformer blocks, int8 per-row on tok_emb/lm_head (not fp16)

**KEY_INNOVATIONS:**
- Mixed quantization scheme: int8 per-row for embedding/head (not fp16 passthrough) — embeddings get quantized more aggressively than fp16 but with per-row scales, trading some precision for better compression
- STE QAT is the real workhorse here — trains the model to be robust to int6 quantization, cutting the quant penalty by ~50×

**WE_DONT_HAVE:**
- Sliding window stride=64 (we use 256) — 4× denser scoring, every token gets near-full 4096 context
- int8 per-row embedding quantization (we passthrough fp16 embed)
- Val-only training mode (separate competition track, not standard)

**WE_SHOULD_STEAL:**
1. **Stride 64 sliding window** (easy, eval-only change): Going from 256→64 gives every token ~960+ context tokens vs ~768. Expected ~0.01–0.02 BPB gain for free — just slower eval (~5× more windows). With our TTT already adding eval cost, measure wall-clock impact first.
2. **int8 per-row embed** (easy, quant-only change): Their embed is quantized to int8 per-row rather than stored as fp16. If we're tight on the 16MB limit, this buys back ~0.5MB (tok_emb is 1024×512 = 512K params; fp16=1MB, int8+scales≈0.53MB). Low risk since embeddings are robust to quantization and per-row scales preserve accuracy.


---
## PR #44 — val-only 10min record (val_bpb:1.1111)
**Author:** daniellawson9999 | **State:** CLOSED | **BPB:** 1.1111 | **Diff:** 1139 lines

TECHNIQUES:
- [TRAIN] val-only training: Routes `train_files` to the validation shard so the model memorizes exactly the data it's evaluated on. Delta: significant (claimed 1.1111 BPB vs ~1.18+ baseline)
- [EVAL] standard BPB eval: Same sentencepiece-based BPB metric as baseline, no changes
- [TRAIN] standard baseline stack: Otherwise identical to the reference `train_gpt.py` — same arch, Muon, LR schedule, int8+zlib export

KEY_INNOVATIONS:
- Trains exclusively on val data — the model overfits the evaluation set by design, which is apparently rule-legal per organizer Discord confirmation
- No architectural changes; the entire gain comes from data leakage into the eval metric

WE_DONT_HAVE:
- Val-only training mode (trivial to add: point `train_files` glob at `fineweb_val_*.bin`)

WE_SHOULD_STEAL:
1. **Val-only training** (easy) — point `DATA_PATH` or `train_files` at the val shards. The author cautions there may be a separate held-out test set for final scoring; if there is, this technique scores 0 on final eval. **Do not index on this** until the held-out test question is resolved. If confirmed val=test, this is a free ~0.05-0.07 BPB gain on top of whatever our current stack achieves.
2. Nothing else — this PR is architecturally identical to the baseline; there are no technical techniques to steal beyond the data routing trick.

**Bottom line:** This is a rules exploit, not a technical improvement. Our stack is strictly superior in every architectural dimension. The only decision is whether to also point at val data — hold off until the organizers clarify whether final scoring uses a separate held-out set.


---
## PR #135 — Record: OrthoInit + Int6 MLP3x + BigramHash + SmearGate (val_bpb: 1.1539)
**Author:** unnir | **State:** OPEN | **BPB:** 1.1539 | **Diff:** 1481 lines

TECHNIQUES:
- [TRAIN] OrthoInit + muP output scaling: orthogonal init (gain=1.0) for large matrices; output projections (`*.proj`) scaled by `1/sqrt(2*num_layers)` post-init. Delta: unknown in isolation
- [EVAL/TRAIN] Int6 mixed quant + zstd-22: per-row int6 on MLP+attn; fp16 passthrough for tok_emb and last 2 layers' K projections (`blocks.7/8.attn.c_k`). Pre-quant→roundtrip: 1.1696→1.1748 (+0.005 BPB)
- [TRAIN] 3x MLP expansion: already in our stack
- [TRAIN] Optimizer tuning: muon momentum warmup 0.92→0.99 over 1500 steps, warmdown=3000, grad_clip=0.3, weight_decay=0.01, beta2=0.95
- [TRAIN] SmearGate: already in our stack
- [TRAIN] BigramHash Embedding: 4096-bucket XOR hash of (prev, curr) token pair → dim=128 → projected to 512, zero-init, learned scale=0.05. ~590K params
- [EVAL] Sliding window stride=64: each token gets ~1984-token context. Delta vs non-sliding: 1.1748→1.1539 (~0.021 BPB gain)

KEY_INNOVATIONS:
- BigramHash uses a zero-initialized projection so it starts neutral and can only help; the XOR hash `(36313*t[i]) ^ (27191*t[i-1]) % 4095` is collision-resistant across common pairs
- Eval stride=64 (vs standard non-overlapping) is the single largest source of the reported score gap — it's a measurement improvement, not a model improvement

WE_DONT_HAVE:
- BigramHash Embedding (4096 buckets, bigram_dim=128→512)
- OrthoInit + muP output projection scaling (`1/sqrt(2*L)`)
- Eval stride=64 (we use stride=256)
- FP16 passthrough specifically for last 2 layers' K projections (quant-sensitive attention keys)

WE_SHOULD_STEAL:
1. **Eval stride=64** (Easy) — drop-in change to `EVAL_STRIDE=64`. Accounts for ~0.015+ BPB improvement over our stride=256 with zero training cost. Pure score gain.
2. **BigramHash Embedding** (Medium) — zero-init means it can't hurt convergence; costs ~590K params from size budget but MLP 3x already freed significant headroom. Gives the model explicit token-pair statistics the transformer has to learn implicitly otherwise.


---
## PR #114 — #1 on leaderboard: val_bpb=1.1574 — Int6 + MLP 3x + selective precision + optimi
**Author:** saml212 | **State:** OPEN | **BPB:** 1.1574 | **Diff:** 1252 lines

TECHNIQUES:
- [TRAIN] train_seq_len=2048: Trains at 2048 vs common 1024/4096. Delta: ~0 BPB vs 4096 but more steps in budget
- [TRAIN] grad_clip_norm=0.3: Tight gradient clipping for long-sequence stability. Delta: meaningful at seq_len=2048
- [TRAIN] train_batch_tokens=786K: Swept 393K–1M, found 786K optimal for seq_len=2048. Delta: vs default 524K
- [EVAL] sliding_window eval stride=256: Counter to stride=64, 4x faster with marginal loss. Delta: +0.0005 BPB vs stride=64
- [QUANT] int6 post-training quantization: per-row scaling, ±31 range, ~25% smaller than int8
- [QUANT] fp16 tied embedding passthrough: tok_emb.weight kept fp16, not quantized. From PR #61
- [QUANT] late-K fp16 passthrough: Last 2 layers' c_k.weight kept fp16. From PR #99
- [ARCH] U-Net skip connections: encoder/decoder halves with learned skip_weights
- [ARCH] resid_mix per-block: learned blend of x and x0 (initial embedding)
- [ARCH] MLP 3x expansion with relu²: hidden = 3 * dim
- [ARCH] logit softcap=30: tanh-capped logits
- [OPT] Muon + momentum warmup: starts at 0.85, ramps to 0.95 over 500 steps

KEY_INNOVATIONS:
- **seq_len=2048 sweet spot**: sliding window eval already provides long context; training at 2048 gives more steps than 4096 with identical BPB, strictly dominating 4096
- **grad_clip=0.3 for long sequences**: narrow optimum found via sweep; longer sequences have higher gradient variance requiring tighter clipping than the common 1.0

WE_DONT_HAVE:
- `train_seq_len=2048` — we appear to be at 1024
- `grad_clip_norm=0.3` — we have grad clipping but likely not tuned to this value for seq_len=2048
- `train_batch_tokens=786K` — we're likely at 524K default
- late-K fp16 passthrough (last 2 layers' c_k.weight in fp16) — we have fp16 embed but not this

WE_SHOULD_STEAL:
1. **train_seq_len=2048 + grad_clip=0.3 + batch=786K** (medium): These three are coupled — saml212 swept them together and they interact. The claim is free BPB improvement since 2048 gets the same quality as 4096 but with more gradient steps. Expected delta: ~0.01–0.02 BPB. Requires re-tuning batch/clip together.
2. **late-K fp16 passthrough** (easy): Keep last 2 layers' c_k.weight in fp16 post-quantization. We already have fp16 embed passthrough; this is a one-liner addition to `quantize_state_dict_int8`. Low cost, directly addresses quantization damage in late-layer attention sharpness.


---
## PR #122 — Record: Sliding Window Eval, 2048 Vocab Size, fp16 embeddings, SWA, NorMuon, FA3
**Author:** mtybadger | **State:** OPEN | **BPB:** 1.1585 | **Diff:** 1509 lines

TECHNIQUES:
- [TRAIN] NorMuon: Second-moment adaptive Muon variant — orthogonalizes gradients then applies per-row AdaGrad-like scaling. Delta: unknown vs plain Muon
- [TRAIN] int6 row-wise QAT + STE: Weights fake-quantized to int6 during training with straight-through estimator on backward. Delta: enables 6-bit compression without post-hoc gap
- [TRAIN] fp16 embeddings: tok_emb kept in fp16, bypassing int6 quantization path. Delta: reduces quant gap for embedding-heavy vocab
- [TRAIN] SWA (swa_start_frac=0.5, swa_every=200): Averages checkpoints from second half of training. Delta: small regularization benefit
- [TRAIN] Vocab size 2048 + sp2048 tokenizer: Custom SentencePiece tokenizer, trades one transformer layer for larger vocab
- [EVAL] Sliding window eval, stride=64, seqlen=1024: Scores only final `stride` tokens per window; small stride = dense context coverage. Delta: meaningful BPB improvement vs stride-independent eval
- [TRAIN] FlashAttention 3: Replaces FA2/SDPA. Delta: ~10ms/step saved on H100

KEY_INNOVATIONS:
- NorMuon adds a second momentum buffer per-parameter for adaptive step scaling after Newton-Schulz orthogonalization — more stable than vanilla Muon at high momentum (0.99)
- Stride=64 sliding window eval is aggressive: with seqlen=1024, every token except the first 960 gets scored with near-full context. This is an eval methodology choice, not a model improvement

WE_DONT_HAVE:
- Eval stride 64 (we use 256) — their sliding window is 4× denser; measurable BPB gain from better context utilization at eval time
- FlashAttention 3 — they explicitly claim ~10ms/step free lunch on H100s

WE_SHOULD_STEAL:
1. **Eval stride 64** (easy): Pure eval change, no training impact. With seqlen=1024 and stride=64, every scored token has 960 tokens of context. Expected BPB drop of ~0.003–0.008 based on typical sliding window gains in this competition. Zero training cost.
2. **FA3** (easy, if not already wired in): If our stack isn't already using `flash_attn_interface`, dropping it in saves ~10ms/step → ~3 min recovered on an 8×H100 10-min run, which translates to ~200–300 more gradient steps. Check whether our `CausalSelfAttention` already calls `_fa3_func`.


---
## PR #128 — Record: Int6 MLP3x + STE QAT + Sliding Window (val_bpb=1.1594)
**Author:** rsavitt | **State:** OPEN | **BPB:** 1.1594 | **Diff:** 1361 lines

TECHNIQUES:
- [TRAIN] int6_per_row_QAT: STE fake-int6 quantization in CastedLinear forward pass; gradients flow through unquantized weights. Delta: ~0.01-0.02 BPB vs post-training quant only
- [TRAIN] mlp_3x_expansion: MLP hidden=1536 (3× model_dim), enabled by int6 space savings. Delta: meaningful vs 2x baseline
- [EVAL] sliding_window_stride64: Stride=64 with seq_len=4096; each token scored with near-maximum context. Delta: ~0.01+ BPB vs non-sliding eval
- [QUANT] int6_zstd22: Per-row int6 + zstd level-22 compression. Saves ~4MB vs int8+zlib
- [QUANT] fp16_embed_passthrough: tok_emb.weight kept fp16 through quantization, not int6-quantized
- [TRAIN] hyperparams: matrix_lr=0.02, muon_momentum=0.99, warmdown=3000

KEY_INNOVATIONS:
- STE QAT baked directly into CastedLinear.forward — clean, no separate training phase; weights learn to be int6-friendly end-to-end
- Eval stride=64 (vs typical 256+) dramatically increases context coverage per token at cost of ~4× eval time

WE_DONT_HAVE:
- Eval stride 64 (we use 256) — this is a pure scoring improvement; same model, lower reported BPB due to more context per scored token

WE_SHOULD_STEAL:
- **Eval stride 64** (easy): Drop-in change to `EVAL_STRIDE=64`. No model changes. Expected ~0.005-0.015 BPB improvement from better context coverage. Only cost is longer eval (~4× their 207s). If we're compute-limited on eval, try stride=128 as a middle ground.
- **Nothing else** — int6+zstd, STE QAT, MLP 3x, fp16 embed are all already in our stack. Their hyperparam values (matrix_lr=0.02, momentum=0.99) are worth benchmarking if our current values differ.


---
## PR #156 — feat(record): Int6 STE + NorMuon + SWA + Sliding Window (val_bpb=1.16019)
**Author:** dexhunter | **State:** OPEN | **BPB:** 1.16019 | **Diff:** 1268 lines

TECHNIQUES:
- [TRAIN] Int6 STE: Fake per-row int6 quantization ([-31,31]) on every forward pass via straight-through estimator in CastedLinear. Delta: ~+0.002 BPB gap vs unquantized
- [TRAIN] NorMuon: Muon variant with row-normalized Newton-Schulz + per-row second momentum (AdaGrad-style step scaling). Delta: unknown vs standard Muon
- [TRAIN] SWA: Checkpoint averaging during warmdown (every 200 steps, starting at 50% through warmdown, ~7 checkpoints). Delta: unknown
- [TRAIN] 3x MLP width (hidden=1536): Enabled by int6 compression budget savings
- [TRAIN] FP16 tied embedding: tok_emb stored fp16, excluded from int6 STE. Delta: unknown
- [EVAL] Sliding window stride=64: Much tighter than typical. Delta: ~0.033 BPB per description
- [TRAIN] U-Net skip connections: Encoder/decoder halves with learnable per-dim skip weights
- [EVAL] Zstd-22 compression: Better ratio than zlib for quantized weights

KEY_INNOVATIONS:
- NorMuon adds per-row second momentum to Muon's Newton-Schulz orthogonalization — adaptive step sizes without breaking the spectral structure that makes Muon effective
- STE QAT at int6 (not int8) means the model actively learns to tolerate quantization noise, making compression essentially lossless at eval time

WE_DONT_HAVE:
- **NorMuon** (we use standard Muon — this adds per-row adaptive scaling via second momentum buffer)
- **SWA with stride=64 eval** (we have SWA and sliding window but our stride is 256, not 64 — theirs is 4x tighter)
- **Momentum warmup** (linearly ramping muon momentum from 0.92→0.99 over first 1500 steps)

WE_SHOULD_STEAL:
1. **Eval stride=64 instead of 256** — their PR description credits ~0.033 BPB to sliding window; tighter stride captures more context per token at no training cost. **Easy** (single hyperparameter change), high impact.
2. **NorMuon over standard Muon** — adds a second momentum buffer (per-row, shape `[rows, 1]`) that adaptively scales updates. Low memory overhead, potentially meaningful BPB gain. **Medium** (need to benchmark vs our Muon tuning, may require LR re-sweep).


---
## PR #99 — submission: Int6 MLP3x + Late-K Passthrough + SlidingWindow (val_bpb: 1.1605)
**Author:** takhir-iota | **State:** OPEN | **BPB:** 1.1605 | **Diff:** 463 lines

TECHNIQUES:
- [TRAIN] Int6 mixed quantization: `.mlp.`, `.attn.c_q.`, `.attn.c_v.`, `.attn.proj.` stored in int6 (LOWBIT_BITS=6). Delta: unknown vs int8 baseline
- [TRAIN] zstd compression: SERIAL_COMPRESSOR=zstd level=22 instead of zlib. Delta: smaller file size
- [TRAIN] MLP_MULT=3: wider MLP hidden layer. Delta: positive within byte budget
- [TRAIN] Selective K preservation: blocks.7 and blocks.8 `.attn.c_k.weight` kept in fp16; remaining c_k uses grouped int8 with group_size=64. Delta: ~0.002 BPB claimed
- [EVAL] Sliding-window eval stride=64: near-full context at eval time. Delta: ~0.022 BPB vs stride=256 (1.1605 vs ~1.183)

KEY_INNOVATIONS:
- Late-layer K passthrough: selectively preserving c_k weights in fp16 only for the last 2 layers, treating attention keys asymmetrically from Q/V
- Sliding-window stride=64 (we use stride=256): the tighter stride is the single largest claimed improvement

WE_DONT_HAVE:
- `EVAL_STRIDE=64` (we use stride=256) — this is a pure eval-time gain, no training cost
- Selective late-layer K preservation: `INT8_KEEP_FLOAT_NAME_PATTERNS=tok_emb.weight,blocks.7.attn.c_k.weight,blocks.8.attn.c_k.weight` with `INT8_GROUP_OVERRIDES=.attn.c_k.:64` for the rest
- zstd compression (we use zlib) — already in our codebase but not in our active config

WE_SHOULD_STEAL:
1. **EVAL_STRIDE=64** (easy): Pure eval change, no retraining needed. ~0.02 BPB gain vs our current stride=256. Drop-in config change.
2. **Selective late-K fp16 passthrough** (easy): Keep only blocks 7+8 c_k in fp16, group-quantize the rest at group_size=64. Asymmetric treatment of K vs Q/V is well-motivated since K affects attention pattern directly. Low risk, likely 0.003–0.005 BPB.


---
## PR #88 — Record: Int6 MLP3x + MTP + Sliding Window Eval (val_bpb=1.1605)
**Author:** seanward | **State:** OPEN | **BPB:** 1.1605 | **Diff:** 1509 lines

TECHNIQUES:
- [TRAIN] int6_per_row_quant: Quantize 2D weights to [-31,31], stored int8, zstd-22 compressed. Delta: ~25% artifact savings vs int8+zlib
- [TRAIN] mlp_3x: MLP_HIDDEN=1536 (3× model_dim=512), enabled by int6 space savings. Delta: significant
- [TRAIN] seq4096: TRAIN_SEQ_LEN=4096 — 4× longer context during training. Delta: unknown, claimed key enabler
- [TRAIN] mtp_aux_head: 1 extra head predicts token i+2, weight 0.1, excluded from artifact. Delta: small
- [EVAL] fp16_tied_embed: Embedding exported as fp16, bypasses int8 quant gap. Delta: small
- [EVAL] sliding_window_stride512: Score only last stride tokens per window, near-full context. Delta: ~0.01 BPB vs chunked
- [TRAIN] optimizer_tuning: MATRIX_LR=0.02, MUON_MOMENTUM=0.99, WARMDOWN_ITERS=3000. Delta: co-optimized, unquantified

KEY_INNOVATIONS:
- int6+zstd as a deliberate space trade: lower precision buys a wider MLP that more than compensates in quality
- seq4096 training with sliding window eval at stride 512 — longer context both during training and scoring

WE_DONT_HAVE:
- **seq4096 training** (TRAIN_SEQ_LEN=4096) — their PR uses 4× longer sequences; we're at 1024
- **WARMDOWN_ITERS=3000** — they use 2.5× longer warmdown than default (1200); our value unknown
- **MUON_MOMENTUM=0.99** — higher than their default 0.95; may interact with our NorMuon differently

WE_SHOULD_STEAL:
1. **seq4096 training** — longer context is architecturally free (no extra params, no artifact cost), and directly benefits our sliding window eval. Our TTT also likely improves with longer prefix context. **Impact: medium-high (~0.005–0.015 BPB), Difficulty: easy** (env var change, watch for memory/throughput regression on 8×H100)
2. **WARMDOWN_ITERS=3000 + MUON_MOMENTUM=0.99** — their optimizer co-tuning is cheap to test and may compound with our NorMuon. **Impact: small-medium, Difficulty: easy** (two env vars, run a seed sweep)


---
## PR #102 — Int6 MLP3x + Tuned LR + SmearGate + SlidingWindow (val_bpb: 1.1618)
**Author:** unnir | **State:** OPEN | **BPB:** 1.1618 | **Diff:** 1481 lines

TECHNIQUES:
- [TRAIN] int6_mixed_quant+zstd22: Per-row int6 for MLP/attn weights, fp16 passthrough for tied embeddings, zstd level 22. Delta: ~4MB savings vs int8+zlib, enables wider MLP
- [TRAIN] mlp_3x: MLP hidden = 3×model_dim (1536). Delta: ~0.019 BPB
- [TRAIN] tuned_lr: matrix_lr=0.02, scalar_lr=0.02, tied_embed_lr=0.03 (halved from baseline)
- [TRAIN] muon_momentum_warmup: Momentum ramps 0.92→0.99 over first 1500 steps
- [TRAIN] warmdown_3000: Extended warmdown from default to 3000 iters
- [TRAIN] grad_clip_norm=1.0: Gradient norm clipping
- [TRAIN] SmearGate: Learned blend of current+prev token embedding before layer 0. ~512 params. Delta: small
- [EVAL] sliding_window_stride64: Each token scored with ~960-token context (stride=64). Delta: ~0.03 BPB vs non-overlapping. Eval cost: ~64s on 8xH100

KEY_INNOVATIONS:
- Stride=64 sliding window is unusually aggressive — they're trading eval time (64s) for a clean ~0.03 BPB gain that requires zero training changes
- Muon momentum warmup (cold start at 0.92) avoids early training instability from high momentum before the optimizer has meaningful history

WE_DONT_HAVE:
- Stride=64 sliding window eval (we use stride=256; their stride gives significantly more context per scored token)
- Muon momentum warmup 0.92→0.99 over 1500 steps (we use fixed momentum)

WE_SHOULD_STEAL:
1. **Stride=64 eval** (Easy, ~0.03 BPB): Pure eval change, zero training cost, just slower final eval. We're leaving ~0.02+ BPB on the table with stride=256 vs 64. Drop-in change.
2. **Muon momentum warmup** (Easy, small delta): Costs nothing, stabilizes early training. Worth folding in given we already use Muon — likely marginal but free.


---
## PR #89 — record:  val_bpb=1.1622, NorMuon + int6 STE + SWA + sliding window
**Author:** vmfunc | **State:** OPEN | **BPB:** 1.1622 | **Diff:** 1262 lines

TECHNIQUES:
- [TRAIN] int6 STE QAT: fake per-row int6 quantization during forward pass with straight-through estimator. Delta: ~+0.002 BPB quant gap (very small)
- [TRAIN] fp16 embedding passthrough: tok_emb kept fp16, skipped from quantization entirely. Delta: implicit (part of int6 story)
- [TRAIN] MLP 3x (hidden=1536): wider MLP enabled by int6 compression savings freeing artifact bytes
- [TRAIN] NorMuon: row-normalized Newton-Schulz updates with second-moment normalization on top of standard Muon. Delta: not isolated
- [TRAIN] SWA: average 7 checkpoints during warmdown (every 200 steps, starting at 50% through warmdown). Delta: not isolated
- [EVAL] sliding window stride=64: each scored token gets 960 tokens of context. Delta: ~0.033 BPB improvement vs non-sliding

KEY_INNOVATIONS:
- NorMuon adds a second-moment buffer (per-row, shape `[rows, 1]`) to Newton-Schulz updates, normalizing step magnitude — standard Muon has no second-moment term
- STE int6 clip uses `torch.quantile(..., dim=1)` per-row (not per-tensor), so scale adapts to each row's distribution; STE wires gradients through the round() op

WE_DONT_HAVE:
- NorMuon (we have Muon, but not the second-moment row-normalization variant)
- SWA during warmdown (we have no checkpoint averaging)
- Sliding window stride=64 (we have stride=256 — they use 4x denser stride)

WE_SHOULD_STEAL:
1. **Stride 64 sliding window** (easy, ~0.033 BPB): purely an eval change, no training impact. We're at stride=256; dropping to 64 is free BPB. Only cost is 4x longer eval time.
2. **NorMuon second-moment normalization** (medium, unknown delta but likely 0.005–0.015 BPB): adds one small buffer per matrix param, minimal memory overhead. The key change is `second_momentum_buffer` shape `[rows, 1]` tracking `E[v²]` per row — bolt-on to existing Muon step.


---
## PR #160 — Record: MLP3x + Int8 Tok Emb + Grouped LZMA + Sliding Window (val_bpb=1.1623)
**Author:** ChaseWNorton | **State:** OPEN | **BPB:** 1.1623 | **Diff:** 1463 lines

TECHNIQUES:
- [TRAIN] MLP 3x: feedforward expansion 2x→3x. Delta: known baseline improvement, already in our stack
- [TRAIN] seq_len=2048: train and eval at 2048 context. Delta: part of our stack
- [TRAIN] GQA 8h/4kv: grouped-query attention. Delta: in our stack
- [TRAIN] Muon momentum=0.99: standard Muon config. Delta: in our stack
- [EVAL] Sliding window eval (stride=256, seq=2048): reduces prefix-context penalty vs chunked eval. Delta: ~0.013 BPB improvement (1.1753→1.1623). In our stack
- [EVAL] fp16 tok_emb passthrough: skip int6 quantization for embedding table, keep fp16. Delta: small quant quality gain. In our stack
- [EVAL] fp16 last-two-layer c_k passthrough: keep c_k weights of final 2 attention layers in fp16 instead of int6. Delta: unknown but targets high-sensitivity weights
- [EVAL] LZMA compressor (preset=6): replaces zlib. Delta: better compression ratio than zlib, trades compress speed for size
- [TRAIN] QAT scaffolding present but NOT activated: run stopped at step 7534, QAT start was step 10000. Final result is cold-quant only.

KEY_INNOVATIONS:
- fp16 passthrough for last-2-layer c_k weights during quantization — targeted precision preservation for the most context-sensitive projection matrices
- Compressor swap from zlib→lzma gets more bytes under the 16MB cap without changing model quality

WE_DONT_HAVE:
- `fp16_late_k_layers=2`: last 2 layers' c_k kept in fp16 during export (we only do fp16 for tok_emb, not late-layer keys)
- LZMA compression (we use zstd; lzma preset=6 may compress int6 weight data differently — worth benchmarking against zstd at equivalent levels)

WE_SHOULD_STEAL:
- **fp16 late-K-layers passthrough** (easy): add `fp16_late_k_layers=2` to our quant export — last-layer key projections carry the most positional signal and are highest-loss under int6. One env var, zero training change. Expected: 0.001–0.003 BPB recovery after quantization.
- **Nothing else** — their score (1.1623) is weaker than what our full stack (NorMuon + SWA + SmearGate + MTP + TTT) should already achieve. This PR is essentially the public baseline + sliding window, which we already have. The only novel bit is the c_k fp16 passthrough.


---
## PR #65 — Record: Mixed Quant (int6+int8) + Sliding Window, val_bpb=1.1630
**Author:** aquariouseworkman | **State:** OPEN | **BPB:** 1.163 | **Diff:** 1331 lines

TECHNIQUES:
- [TRAIN] STE QAT int6: Fake int6 per-row quantization in `CastedLinear.forward()` during training so model learns to be robust to post-training quant. Delta: reduces quant penalty to +0.0015 BPB. **WE HAVE THIS**
- [QUANT] Mixed precision: int8 (127 levels) for `tok_emb`, int6 (31 levels) for all other 2D weights. Rationale: embedding has no STE fake-quant in forward pass so needs gentler quant. **WE HAVE fp16 passthrough for embed instead** (higher quality, more bytes)
- [EVAL] Sliding window eval stride=64: each scored token sees 960 tokens of context. Delta: ~0.034 BPB claimed. **WE HAVE stride=256** (less context per token)
- [TRAIN] MLP_MULT=3, seq_len=1024, 524K tokens/step: standard throughput config. **WE HAVE THIS**

KEY_INNOVATIONS:
- Mixed quant rationale is principled: int8 for layers without STE protection (embedding), int6 for STE-trained layers. Not just "try different precisions" — tied to whether fake-quant runs during training.
- Stride=64 sliding window is aggressive (15x more windows than non-overlapping), trading eval time for BPB.

WE_DONT_HAVE:
- **Sliding window stride=64** — we use stride=256. Finer stride gives each token ~960 tokens of context vs ~768 with stride=256. Pure eval improvement, zero training change.

WE_SHOULD_STEAL:
1. **Reduce sliding window stride to 64** (easy) — we already have the mechanism, just change the hyperparameter. Should yield a few thousandths of BPB improvement. Only cost is longer eval time.
2. **Nothing else** — their mixed quant (int8 embed) is strictly worse than our fp16 embed passthrough in quality terms; we already have STE QAT, MLP_MULT=3, and everything else. Their 1.1630 BPB is behind our stack.


---
## PR #123 — Record: Vocab 4096 + MLP 3x + Sliding Window Eval (mean val_bpb=1.1642, 3 seeds)
**Author:** saikrishnarallabandi | **State:** OPEN | **BPB:** 1.1642 | **Diff:** 1411 lines

TECHNIQUES:
- [TRAIN] vocab_4096: Custom SentencePiece BPE tokenizer at 4096 vocab; more bytes/token directly improves BPB. Delta: ~0.03-0.05 BPB (dominant contributor, per author)
- [TRAIN] seq4096: TRAIN_SEQ_LEN=4096, 4x longer context windows during training. Delta: unknown isolated
- [TRAIN] int6_ste_qat: Per-row int6 fake-quant with STE on CastedLinear weights during training. Delta: +0.005 BPB penalty vs unquantized
- [TRAIN] int8_embed: tok_emb quantized at 8-bit (vs weight int6) to reclaim size budget. Delta: marginal vs fp16 embed
- [TRAIN] swa_7ckpt: Stochastic weight averaging over warmdown checkpoints (every 200 steps, start_frac=0.5). Delta: small
- [EVAL] sliding_window_stride256: Overlapping eval windows at stride=256 over seq_len=4096. Delta: ~0.002-0.005 BPB vs non-overlapping
- [TRAIN] muon_momentum_warmup: Momentum ramp 0.92→0.99 over 1500 steps. Delta: unknown isolated
- [TRAIN] 8L not 9L: Uses 8 layers (vs standard 9), offset by wider MLP (3x). Delta: architectural tradeoff

KEY_INNOVATIONS:
- Vocab 4096 is the primary lever — bigger tokenizer compresses more bytes/token, directly cutting BPB independent of model quality
- Int6 weights + int8 embed combination threads the 16MB needle: int6 shrinks transformer matrices enough to add MLP 3x headroom, int8 (not fp16) on embeddings keeps vocab table manageable

WE_DONT_HAVE:
- Vocab 4096 tokenizer (our stack is silent on vocab — baseline is 1024; this is likely our gap)
- TRAIN_SEQ_LEN=4096 (longer context training, not just longer eval)
- Int8 embed quantization (we have fp16 embed, which is larger; they fit vocab table under budget differently)

WE_SHOULD_STEAL:
1. **Vocab 4096** — largest single BPB lever in the competition so far; tokenizer + dataset are pre-built at `sproos/parameter-golf-tokenizers`. Easy to drop in (env var swap + data re-prep). Expected: ~0.03-0.05 BPB. Difficulty: **medium** (need dataset re-tokenized, already done by them)
2. **TRAIN_SEQ_LEN=4096** — longer context lets the model see more dependencies per step, complements sliding window eval. Expected: ~0.01-0.02 BPB. Difficulty: **easy** (single env var, but increases memory pressure — may need to verify it fits our 9L+MTP+TTT stack)


## Technique Inventory

| Technique | Type | Best BPB | PRs | In our stack? | Steal priority |
|---|---|---|---|---|---|
| Sliding window stride=64 | EVAL | 0.9588 | 120,64,135,122,128,156,99,102,89,65 | ❌ (stride=256) | **P1** |
| MLP 3x expansion | ARCH | 0.9588 | all | ✅ | — |
| STE int6 QAT | TRAIN | 0.9588 | 120,64,128,156,89,65 | ✅ | — |
| int6 per-row quant + zstd | QUANT | 0.9588 | most | ✅ | — |
| fp16 embed passthrough | QUANT | 0.9588 | most | ✅ | — |
| NorMuon | TRAIN | 1.16019 | 122,156,89 | ✅ | — |
| SWA (warmdown ckpt avg) | TRAIN | 1.16019 | 122,156,89,123 | ✅ | — |
| SmearGate | ARCH | 1.1539 | 135,102 | ✅ | — |
| MTP auxiliary head | TRAIN | 1.1605 | 88 | ✅ | — |
| TTT | TRAIN | — | none | ✅ (unique) | — |
| Late-K fp16 passthrough (last 2 layers c_k) | QUANT | 1.1574 | 114,99,160 | ❌ | **P2** |
| train_seq_len=4096 | TRAIN | 1.1605 | 88,123 | ❌ | **P3** |
| train_seq_len=2048 | TRAIN | 1.1574 | 114 | ❌ | P4 |
| Muon momentum warmup 0.92→0.99/1500 | TRAIN | 1.1539 | 120,135,102,123 | ❌ | P5 |
| BigramHash embedding | ARCH | 1.1539 | 135 | ❌ | P6 |
| U-Net skip connections | ARCH | 1.1574 | 114,156 | ❌ | P7 |
| OrthoInit + muP output scaling | TRAIN | 1.1539 | 135 | ❌ | P7 |
| logit softcap=30 | ARCH | 1.1574 | 114 | ❌ | P8 |
| resid_mix per-block | ARCH | 1.1574 | 120,114 | ❌ | P8 |
| Vocab 4096 tokenizer | ARCH | 1.1642 (3-seed) | 123 | ❌ | P3† |
| Vocab 2048 tokenizer | ARCH | 1.1585 | 122 | ❌ | P4† |
| FA3 | TRAIN | 1.1585 | 122 | ❓ | P5 |
| grad_clip=0.3 | TRAIN | 1.1574 | 114 | ❓ | P6 |
| warmdown=3000 | TRAIN | 1.1594 | 88,102,128 | ❓ | P5 |
| Val-only training | DATA | 0.9695 | 64,44 | ❌ | ⚠️ rules exploit |
| LZMA compression | QUANT | 1.1623 | 160 | ❌ | low |
| int8 embed (vs fp16) | QUANT | 1.1623 | 64,123 | ❌ | skip (worse than fp16) |

---

## Top 5 Things to Steal

**1. Eval stride=64** (was stride=256)
- Delta: **~0.02–0.033 BPB** — single largest free gain remaining
- Difficulty: trivial (one env var / loop param)
- Evidence: 10+ PRs independently confirm; PR #102 claims 0.03 BPB, PR #156 claims 0.033
- Cost: ~4× longer eval; with TTT already slow, measure wall-clock on 4090

**2. Late-K fp16 passthrough** (blocks.7 + blocks.8 c_k.weight)
- Delta: **~0.002–0.005 BPB** post-quant recovery
- Difficulty: easy (add 2 tensor names to fp16 passthrough list, already have the mechanism)
- Reference: PR #114, #99, #160 — three independent submissions converge on same layer range
- Rationale: late-layer keys encode the most positional/contextual signal; int6 damages them disproportionately

**3. train_seq_len=4096** (if we're at 1024; or 2048 per PR #114)
- Delta: **~0.01–0.02 BPB**; PR #88 treats it as a key enabler for sliding window quality
- Difficulty: easy (env var), but watch memory pressure with 9L+MTP+TTT stack on 4090
- Note: PR #114 argues 2048 strictly dominates 4096 (same quality, more steps) — worth testing both

**4. Muon momentum warmup 0.92→0.99 over 1500 steps**
- Delta: **small but free** — stabilizes early training, 4 independent PRs use it
- Difficulty: trivial (ramp schedule in optimizer init)
- Note: We have NorMuon; warmup may be less critical but still a free hedge against early instability

**5. BigramHash embedding** (4096 buckets, bigram_dim=128→512, zero-init)
- Delta: **unknown but principled** — zero-init means it can't hurt; PR #135 is the only submission that uses it and scores 1.1539 with stride=64
- Difficulty: medium (~30 lines, new embedding table + projection)
- Risk: ~590K params from size budget; verify headroom with int6+MTP+TTT artifact size

---

## Approaches Only 1–2 PRs Use (potential alpha)

| Technique | PRs | Why interesting |
|---|---|---|
| **BigramHash embedding** | #135 only | Zero-init = safe; gives model explicit bigram statistics without hurting convergence. No one else is using it. |
| **U-Net skip connections** | #114, #156 | Encoder/decoder halves with learned skip weights. PR #114 is the current public #1 (1.1574). May compound with SmearGate. |
| **OrthoInit + muP output scaling** | #135 only | `1/sqrt(2*L)` output projection scaling is a principled muP init. One-time init change, no ongoing cost. |
| **logit softcap=30** | #114 only | tanh capping prevents logit explosion at long context — may interact positively with our seq4096 + sliding window. |
| **TTT** | us only | No competing PR uses test-time training. Our TTT is genuine model quality alpha — not a measurement trick. |

The key asymmetry: **stride=64 is a measurement improvement disguised as a model improvement** — most of the visible BPB gap between us and the top PRs evaporates once we match their eval methodology. Our actual model (TTT + NorMuon + SWA + MTP + SmearGate) is architecturally ahead of every PR here except possibly PR #114's U-Net + resid_mix stack.
